# SYNTHETIC FALLBACK — not biological data

512 artificial nodes and 3072 directed edges. Used solely for offline tests and pipeline demonstrations. No connection was measured in a fly. Reproduce using `python scripts/make_synthetic_sample.py`. Manifest records the seed and hashes.
