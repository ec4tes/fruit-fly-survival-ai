# Offline wiring check for the revised motion/reward settings; never learning evidence.
extends: smoke.yaml
output_dir: results/learning_v2_smoke
environment:
  turn_speed_fraction: 0.65
  idle_energy_cost: 3.0
  sprint_energy_cost: 5.0
reward:
  food: 10.0
  food_potential: 20.0
  heading_potential: 2.0
  collision: -0.1
  capture: -10.0
  starvation: -10.0
training:
  ent_coef: 0.03
