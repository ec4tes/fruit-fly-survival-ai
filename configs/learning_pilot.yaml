# Development protocol: do not tune against the final evaluation seed set.
extends: connectome.yaml
output_dir: results/learning_pilot
environment:
  turn_speed_fraction: 0.65
  idle_energy_cost: 3.0
  sprint_energy_cost: 5.0
reward:
  food: 10.0
  food_potential: 20.0
  collision: -0.1
  capture: -10.0
  starvation: -10.0
training:
  validation_seeds: []
  timesteps: 16384
  ent_coef: 0.03
experiments:
  train_seeds: [42]
  eval_seeds: [20001, 20002, 20003, 20004, 20005]
