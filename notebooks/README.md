# Analysis without hidden notebook state

The canonical analysis runs through `fly-ai experiments` and `fly-ai plots`.
Explore the resulting CSV files in a notebook if useful, but keep training,
preprocessing and figure generation in the tested Python modules. No notebook
execution is required to reproduce the project.
